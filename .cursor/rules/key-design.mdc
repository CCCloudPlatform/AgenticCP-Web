---
alwaysApply: true
---
# AgenticCP-Web Next-Gen Minimal Design System Rules

## 🎨 디자인 시스템 개요
이 프로젝트는 차세대 미니멀 디자인 시스템을 사용합니다. 모든 스타일링은 일관성과 확장성을 고려하여 설계되었습니다.

## 🎯 핵심 원칙

### 1. 미니멀리즘
- 불필요한 요소 제거
- 깔끔하고 직관적인 레이아웃
- 명확한 정보 계층 구조

### 2. 차세대 감각
- 현대적인 색상 팔레트 사용
- 부드러운 그라데이션과 글래스 효과
- 미묘한 애니메이션과 트랜지션

### 3. 일관성
- CSS 변수를 통한 디자인 토큰 활용
- 통일된 간격, 타이포그래피, 컴포넌트 스타일
- 반응형 디자인 원칙 준수

## 🎨 색상 시스템

### Primary Colors
```scss
--color-primary: #6366f1;        // 메인 브랜드 컬러
--color-primary-light: #8b5cf6;  // 라이트 버전
--color-primary-dark: #4f46e5;   // 다크 버전
```

### Neutral Colors
```scss
--color-gray-50: #f8fafc;   // 가장 밝은 회색
--color-gray-100: #f1f5f9;  // 배경색
--color-gray-200: #e2e8f0;  // 경계선
--color-gray-300: #cbd5e1;  // 비활성 요소
--color-gray-400: #94a3b8;  // 플레이스홀더
--color-gray-500: #64748b;  // 보조 텍스트
--color-gray-600: #475569;  // 일반 텍스트
--color-gray-700: #334155;  // 강조 텍스트
--color-gray-800: #1e293b;  // 제목 텍스트
--color-gray-900: #0f172a;  // 가장 어두운 텍스트
```

### Semantic Colors
```scss
--color-success: #10b981;   // 성공 상태
--color-warning: #f59e0b;   // 경고 상태
--color-error: #ef4444;     // 에러 상태
--color-info: #3b82f6;      // 정보 상태
```

## 📏 간격 시스템

### Spacing Scale (4px 기준)
```scss
--space-1: 0.25rem;   // 4px
--space-2: 0.5rem;    // 8px
--space-3: 0.75rem;   // 12px
--space-4: 1rem;      // 16px
--space-5: 1.25rem;   // 20px
--space-6: 1.5rem;    // 24px
--space-8: 2rem;      // 32px
--space-10: 2.5rem;   // 40px
--space-12: 3rem;     // 48px
--space-16: 4rem;     // 64px
--space-20: 5rem;     // 80px
--space-24: 6rem;     // 96px
```

## 🔤 타이포그래피

### Font Families
```scss
--font-family-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
--font-family-mono: 'JetBrains Mono', 'Fira Code', Consolas, monospace;
```

### Font Sizes
```scss
--font-size-xs: 0.75rem;    // 12px
--font-size-sm: 0.875rem;   // 14px
--font-size-base: 1rem;     // 16px
--font-size-lg: 1.125rem;   // 18px
--font-size-xl: 1.25rem;    // 20px
--font-size-2xl: 1.5rem;    // 24px
--font-size-3xl: 1.875rem;  // 30px
--font-size-4xl: 2.25rem;   // 36px
```

### Font Weights
```scss
--font-weight-light: 300;
--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;
```

## 🎭 컴포넌트 스타일링 가이드

### 1. 버튼 스타일
```scss
.btn-primary {
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  border: none;
  border-radius: var(--radius-lg);
  color: var(--color-white);
  font-weight: var(--font-weight-semibold);
  transition: all var(--transition-fast);
  box-shadow: var(--shadow-md);

  &:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow-lg);
  }
}
```

### 2. 카드 스타일
```scss
.card {
  background: var(--bg-primary);
  border-radius: var(--radius-xl);
  border: 1px solid var(--border-light);
  box-shadow: var(--shadow-sm);
  padding: var(--space-6);
  transition: all var(--transition-normal);

  &:hover {
    box-shadow: var(--shadow-md);
    transform: translateY(-1px);
  }
}
```

### 3. 입력 필드 스타일
```scss
.input {
  width: 100%;
  height: 48px;
  padding: 0 var(--space-4);
  border: 1px solid var(--border-light);
  border-radius: var(--radius-lg);
  background: var(--bg-primary);
  font-size: var(--font-size-base);
  color: var(--text-primary);
  transition: all var(--transition-fast);

  &:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
  }
}
```

## 🌟 특수 효과

### 글래스 효과
```scss
.glass {
  background: var(--bg-glass);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}
```

### 그라데이션 배경
```scss
.gradient-bg {
  background: linear-gradient(135deg, 
    var(--color-primary) 0%, 
    var(--color-primary-light) 50%, 
    var(--color-secondary) 100%);
}
```

## 📱 반응형 디자인

### 브레이크포인트
- Mobile: < 768px
- Tablet: 768px - 1024px
- Desktop: > 1024px

### 반응형 패턴
```scss
@media (max-width: 768px) {
  .component {
    padding: var(--space-4);
    font-size: var(--font-size-sm);
  }
}
```

## 🎯 컴포넌트 개발 규칙

### 1. CSS 변수 우선 사용
- 하드코딩된 값 대신 CSS 변수 사용
- 일관된 디자인 토큰 활용

### 2. 컴포넌트 구조
```scss
.component-name {
  // 기본 스타일
  display: flex;
  padding: var(--space-4);
  
  // 상태별 스타일
  &.active {
    background: var(--color-primary);
  }
  
  // 하위 요소
  .component-child {
    // 스타일
  }
  
  // 반응형
  @media (max-width: 768px) {
    // 모바일 스타일
  }
}
```

### 3. 애니메이션 규칙
- 빠른 상호작용: `--transition-fast` (150ms)
- 일반적인 변화: `--transition-normal` (250ms)
- 느린 변화: `--transition-slow` (350ms)

## 🚫 금지사항

### 1. 하드코딩된 값 사용 금지
```scss
// ❌ 잘못된 예시
.component {
  color: #333;
  padding: 16px;
  border-radius: 8px;
}

// ✅ 올바른 예시
.component {
  color: var(--text-primary);
  padding: var(--space-4);
  border-radius: var(--radius-md);
}
```

### 2. 인라인 스타일 금지
- 모든 스타일은 SCSS 파일에 정의
- 컴포넌트별로 분리된 스타일 파일 사용

### 3. 일관성 없는 네이밍 금지
- BEM 방법론 또는 의미있는 클래스명 사용
- CSS 변수 네이밍 컨벤션 준수

## 📁 파일 구조

```
src/
├── styles/
│   └── global.scss          // 글로벌 스타일 및 CSS 변수
├── components/
│   └── [component]/
│       ├── [Component].tsx
│       └── [Component].scss  // 컴포넌트별 스타일
└── pages/
    └── [page]/
        ├── [Page].tsx
        └── [Page].scss      // 페이지별 스타일
```

## 🔧 개발 도구

### 1. CSS 변수 활용
- 브라우저 개발자 도구에서 실시간 변경 가능
- 다크모드 등 테마 변경 용이

### 2. 유틸리티 클래스
```scss
.flex { display: flex; }
.flex-center { display: flex; align-items: center; justify-content: center; }
.text-center { text-align: center; }
.shadow-md { box-shadow: var(--shadow-md); }
```

## 📋 체크리스트

새로운 컴포넌트를 만들 때 다음 사항을 확인하세요:

- [ ] CSS 변수를 사용했는가?
- [ ] 일관된 간격 시스템을 사용했는가?
- [ ] 적절한 색상 팔레트를 사용했는가?
- [ ] 반응형 디자인을 고려했는가?
- [ ] 접근성을 고려했는가?
- [ ] 애니메이션과 트랜지션을 적절히 사용했는가?
- [ ] 컴포넌트별 스타일 파일을 분리했는가?

## 🎨 디자인 시스템 업데이트

디자인 시스템을 수정할 때는 다음 순서를 따르세요:

1. `global.scss`의 CSS 변수 수정
2. 영향받는 컴포넌트 스타일 업데이트
3. 문서화 업데이트
4. 테스트 및 검증

이 규칙들을 따라 일관되고 확장 가능한 디자인 시스템을 유지하세요.
