---
alwaysApply: true
---

# AgenticCP 멀티 클라우드 플랫폼 개발 가이드

## 🎯 프로젝트 개요
AgenticCP는 차세대 멀티 클라우드 플랫폼으로, React + TypeScript + Vite 기반의 현대적인 웹 애플리케이션입니다.

## 📁 프로젝트 구조

```
src/
├── components/          # 재사용 가능한 UI 컴포넌트
│   ├── common/         # 공통 컴포넌트 (ErrorBoundary, Loading, PageHeader)
│   ├── layout/         # 레이아웃 컴포넌트 (Header, Sidebar, MainLayout)
│   └── [feature]/      # 기능별 컴포넌트
├── pages/              # 페이지 컴포넌트
├── hooks/              # 커스텀 훅
├── services/           # API 서비스 레이어
├── store/              # 상태 관리 (Zustand)
├── types/              # TypeScript 타입 정의
├── utils/              # 유틸리티 함수
├── constants/           # 상수 정의
├── routes/             # 라우팅 설정
└── styles/             # 글로벌 스타일
```

## 🎨 디자인 시스템 규칙

### CSS 변수 사용 필수
```scss
// ✅ 올바른 예시
.component {
  color: var(--text-primary);
  padding: var(--space-4);
  border-radius: var(--radius-lg);
  background: var(--bg-primary);
}

// ❌ 잘못된 예시
.component {
  color: #333;
  padding: 16px;
  border-radius: 8px;
  background: #fff;
}
```

### 컴포넌트 스타일링 패턴
```scss
.component-name {
  // 기본 스타일
  display: flex;
  padding: var(--space-4);
  
  // 상태별 스타일
  &.active {
    background: var(--color-primary);
  }
  
  // 하위 요소
  .component-child {
    // 스타일
  }
  
  // 반응형
  @media (max-width: 768px) {
    // 모바일 스타일
  }
}
```

## ⚛️ React 컴포넌트 개발 규칙

### 1. 컴포넌트 구조
```typescript
import React from 'react';
import { ComponentProps } from '@/types';
import './Component.scss';

interface ComponentProps {
  title: string;
  children?: React.ReactNode;
  onAction?: () => void;
}

const Component: React.FC<ComponentProps> = ({ 
  title, 
  children, 
  onAction 
}) => {
  return (
    <div className="component">
      <h2 className="component-title">{title}</h2>
      {children}
    </div>
  );
};

export default Component;
```

### 2. Props 인터페이스 규칙
- 모든 Props는 명확한 타입 정의 필수
- Optional props는 `?` 사용
- 이벤트 핸들러는 `on` 접두사 사용
- children은 `React.ReactNode` 타입 사용

### 3. 컴포넌트 네이밍
- PascalCase 사용: `UserProfile`, `DashboardCard`
- 기능을 명확히 표현하는 이름 사용
- 접미사 규칙:
  - `Page`: 페이지 컴포넌트 (`DashboardPage`)
  - `Modal`: 모달 컴포넌트 (`ConfirmModal`)
  - `Form`: 폼 컴포넌트 (`LoginForm`)

## 🔧 TypeScript 개발 규칙

### 1. 타입 정의 패턴
```typescript
// 기본 인터페이스
export interface User {
  id: number;
  username: string;
  email: string;
  role: UserRole;
  createdAt: string;
  updatedAt: string;
}

// Union 타입
export type UserRole = 'SUPER_ADMIN' | 'TENANT_ADMIN' | 'CLOUD_ADMIN' | 'DEVELOPER';

// API 응답 타입
export interface ApiResponse<T = unknown> {
  success: boolean;
  data: T;
  message?: string;
  timestamp: string;
}

// 페이지네이션 타입
export interface PagedResponse<T = unknown> {
  content: T[];
  page: number;
  size: number;
  totalElements: number;
  totalPages: number;
  last: boolean;
  hasNext: boolean;
}
```

### 2. 타입 네이밍 규칙
- 인터페이스: PascalCase (`User`, `ApiResponse`)
- 타입 별칭: PascalCase (`UserRole`, `HttpStatus`)
- 제네릭: 단일 대문자 (`T`, `K`, `V`)
- 상수: UPPER_SNAKE_CASE (`API_BASE_URL`)

### 3. 타입 파일 구조
```typescript
// src/types/index.ts
/**
 * Common Types
 */
export interface ApiResponse<T = unknown> { ... }

/**
 * User Types
 */
export interface User { ... }
export type UserRole = ...;

/**
 * Cloud Types
 */
export interface CloudProvider { ... }
export interface Resource { ... }
```

## 🌐 API 서비스 개발 규칙

### 1. 서비스 구조 패턴
```typescript
import { apiRequest } from './api';
import { User, PagedResponse, PaginationParams } from '@/types';

/**
 * User Management Service
 */
export const userService = {
  /**
   * Get all users
   */
  getUsers: (params?: PaginationParams): Promise<PagedResponse<User>> => {
    return apiRequest.get<PagedResponse<User>>('/api/v1/users', { params });
  },

  /**
   * Get user by ID
   */
  getUserById: (id: number): Promise<User> => {
    return apiRequest.get<User>(`/api/v1/users/${id}`);
  },

  /**
   * Create user
   */
  createUser: (data: Partial<User>): Promise<User> => {
    return apiRequest.post<User>('/api/v1/users', data);
  },

  /**
   * Update user
   */
  updateUser: (id: number, data: Partial<User>): Promise<User> => {
    return apiRequest.put<User>(`/api/v1/users/${id}`, data);
  },

  /**
   * Delete user
   */
  deleteUser: (id: number): Promise<void> => {
    return apiRequest.delete<void>(`/api/v1/users/${id}`);
  },
};
```

### 2. API 메서드 네이밍 규칙
- `get[Resource]s`: 목록 조회 (`getUsers`)
- `get[Resource]ById`: 단일 조회 (`getUserById`)
- `create[Resource]`: 생성 (`createUser`)
- `update[Resource]`: 수정 (`updateUser`)
- `delete[Resource]`: 삭제 (`deleteUser`)

### 3. 에러 처리 패턴
```typescript
try {
  const response = await userService.getUsers();
  // 성공 처리
} catch (error) {
  console.error('사용자 목록 조회 실패:', error);
  // 에러 처리
}
```

## 🗃️ 상태 관리 규칙 (Zustand)

### 1. 스토어 구조 패턴
```typescript
import { create } from 'zustand';

interface StoreState {
  // 상태
  data: DataType[];
  isLoading: boolean;
  error: string | null;
  
  // 액션
  fetchData: () => Promise<void>;
  addData: (item: DataType) => void;
  updateData: (id: string, updates: Partial<DataType>) => void;
  deleteData: (id: string) => void;
  setLoading: (loading: boolean) => void;
  clearError: () => void;
}

export const useDataStore = create<StoreState>((set, get) => ({
  data: [],
  isLoading: false,
  error: null,

  fetchData: async () => {
    set({ isLoading: true, error: null });
    try {
      const response = await dataService.getData();
      set({ data: response, isLoading: false });
    } catch (error) {
      set({ 
        error: error instanceof Error ? error.message : '데이터 조회 실패',
        isLoading: false 
      });
    }
  },

  addData: (item) => set((state) => ({ 
    data: [...state.data, item] 
  })),

  updateData: (id, updates) => set((state) => ({
    data: state.data.map(item => 
      item.id === id ? { ...item, ...updates } : item
    )
  })),

  deleteData: (id) => set((state) => ({
    data: state.data.filter(item => item.id !== id)
  })),

  setLoading: (loading) => set({ isLoading: loading }),
  clearError: () => set({ error: null }),
}));
```

### 2. 스토어 네이밍 규칙
- 파일명: `[feature]Store.ts` (`userStore.ts`)
- 훅명: `use[Feature]Store` (`useUserStore`)
- 상태는 명사, 액션은 동사 사용

## 🎣 커스텀 훅 개발 규칙

### 1. 훅 구조 패턴
```typescript
import { useState, useEffect } from 'react';
import { userService } from '@/services/userService';
import { User } from '@/types';

/**
 * User Management Hook
 */
export const useUsers = () => {
  const [users, setUsers] = useState<User[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchUsers = async () => {
    setIsLoading(true);
    setError(null);
    try {
      const response = await userService.getUsers();
      setUsers(response.content);
    } catch (err) {
      setError(err instanceof Error ? err.message : '사용자 조회 실패');
    } finally {
      setIsLoading(false);
    }
  };

  useEffect(() => {
    fetchUsers();
  }, []);

  return {
    users,
    isLoading,
    error,
    refetch: fetchUsers,
  };
};
```

### 2. 훅 네이밍 규칙
- 파일명: `use[Feature].ts` (`useUsers.ts`)
- 훅명: `use[Feature]` (`useUsers`)
- 반환값은 객체 형태로 구조분해할당 가능하게 구성

## 🛠️ 유틸리티 함수 개발 규칙

### 1. 유틸리티 구조 패턴
```typescript
/**
 * Date Formatting Utilities
 */
export const formatDate = (date: Date | string): string => {
  return new Date(date).toLocaleDateString('ko-KR');
};

export const formatRelativeTime = (date: Date | string): string => {
  return dayjs(date).fromNow();
};

/**
 * Number Formatting Utilities
 */
export const formatNumber = (value: number, decimals = 0): string => {
  return value.toLocaleString('ko-KR', {
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  });
};

export const formatCurrency = (value: number, currency = 'KRW'): string => {
  return new Intl.NumberFormat('ko-KR', {
    style: 'currency',
    currency,
  }).format(value);
};
```

### 2. 유틸리티 네이밍 규칙
- 파일명: 기능별로 분리 (`format.ts`, `validator.ts`, `storage.ts`)
- 함수명: 동사 + 명사 (`formatDate`, `validateEmail`)
- 순수 함수로 작성 (부작용 없음)

## 📝 상수 정의 규칙

### 1. 상수 구조 패턴
```typescript
/**
 * Application Constants
 */
export const APP_NAME = import.meta.env.VITE_APP_NAME || 'AgenticCP';
export const APP_VERSION = import.meta.env.VITE_APP_VERSION || '1.0.0';

/**
 * API Constants
 */
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080';
export const API_TIMEOUT = Number(import.meta.env.VITE_API_TIMEOUT) || 30000;

/**
 * Storage Keys
 */
export const STORAGE_KEYS = {
  TOKEN: 'agenticcp_token',
  REFRESH_TOKEN: 'agenticcp_refresh_token',
  USER_INFO: 'agenticcp_user_info',
} as const;

/**
 * Route Paths
 */
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  USERS: '/users',
} as const;
```

### 2. 상수 네이밍 규칙
- UPPER_SNAKE_CASE 사용
- 객체는 `as const` 사용하여 타입 안정성 확보
- 관련 상수는 객체로 그룹화

## 🚦 라우팅 개발 규칙

### 1. 라우트 구조 패턴
```typescript
import { Routes, Route, Navigate } from 'react-router-dom';
import { lazy, Suspense } from 'react';
import PrivateRoute from './PrivateRoute';
import { ROUTES } from '@/constants';

// Lazy load pages
const DashboardPage = lazy(() => import('@/pages/dashboard/DashboardPage'));
const UsersPage = lazy(() => import('@/pages/users/UsersPage'));

const AppRoutes = () => {
  return (
    <Suspense fallback={<Loading />}>
      <Routes>
        {/* Public routes */}
        <Route path={ROUTES.LOGIN} element={<LoginPage />} />

        {/* Protected routes */}
        <Route element={<PrivateRoute />}>
          <Route element={<MainLayout />}>
            <Route path={ROUTES.HOME} element={<Navigate to={ROUTES.DASHBOARD} replace />} />
            <Route path={ROUTES.DASHBOARD} element={<DashboardPage />} />
            <Route path={ROUTES.USERS} element={<UsersPage />} />
          </Route>
        </Route>

        {/* 404 page */}
        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </Suspense>
  );
};
```

### 2. 라우팅 규칙
- 모든 경로는 `ROUTES` 상수 사용
- 페이지 컴포넌트는 lazy loading 적용
- 보호된 라우트는 `PrivateRoute`로 감싸기
- 404 페이지는 마지막에 배치

## 🎨 스타일링 개발 규칙

### 1. SCSS 파일 구조
```scss
/* ===========================================
   COMPONENT NAME
   =========================================== */

.component-name {
  // 기본 스타일
  display: flex;
  padding: var(--space-4);
  
  // 상태별 스타일
  &.active {
    background: var(--color-primary);
  }
  
  // 하위 요소
  .component-child {
    // 스타일
  }
  
  // 반응형
  @media (max-width: 768px) {
    // 모바일 스타일
  }
}
```

### 2. 스타일링 규칙
- CSS 변수 우선 사용
- BEM 방법론 또는 의미있는 클래스명 사용
- 컴포넌트별로 분리된 SCSS 파일 사용
- 반응형 디자인 고려

## 📋 코드 품질 규칙

### 1. 네이밍 컨벤션
- **변수/함수**: camelCase (`userName`, `getUserData`)
- **상수**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **컴포넌트**: PascalCase (`UserProfile`)
- **파일명**: kebab-case (`user-profile.tsx`)

### 2. 주석 규칙
```typescript
/**
 * 사용자 정보를 조회합니다
 * @param userId 사용자 ID
 * @returns 사용자 정보 Promise
 */
export const getUserById = async (userId: number): Promise<User> => {
  return apiRequest.get<User>(`/api/v1/users/${userId}`);
};
```

### 3. 에러 처리 규칙
```typescript
try {
  const result = await apiCall();
  // 성공 처리
} catch (error) {
  console.error('API 호출 실패:', error);
  // 사용자에게 적절한 에러 메시지 표시
  throw error;
}
```

## 🔍 개발 도구 및 설정

### 1. 필수 VS Code 확장
- TypeScript Importer
- ES7+ React/Redux/React-Native snippets
- Prettier - Code formatter
- ESLint

### 2. 코드 포맷팅
- Prettier 설정 사용
- 세미콜론 사용
- 작은따옴표 사용
- 2칸 들여쓰기

### 3. Git 커밋 메시지 규칙
```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅
refactor: 코드 리팩토링
test: 테스트 추가/수정
chore: 빌드 설정 변경
```

## 🚀 배포 및 빌드 규칙

### 1. 환경 변수 관리
```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_NAME=AgenticCP Dev

# .env.production
VITE_API_BASE_URL=https://api.agenticcp.com
VITE_APP_NAME=AgenticCP
```

### 2. 빌드 최적화
- 코드 스플리팅 적용
- 이미지 최적화
- 번들 크기 모니터링

## 📚 참고 자료

### 1. 공식 문서
- [React 공식 문서](https://react.dev/)
- [TypeScript 공식 문서](https://www.typescriptlang.org/)
- [Vite 공식 문서](https://vitejs.dev/)
- [Zustand 공식 문서](https://zustand-demo.pmnd.rs/)

### 2. 디자인 시스템
- 프로젝트 내 `.cursorrules` 파일 참조
- CSS 변수 기반 디자인 토큰 사용

이 가이드를 따라 일관되고 유지보수 가능한 코드를 작성하세요! 🚀# AgenticCP 멀티 클라우드 플랫폼 개발 가이드

## 🎯 프로젝트 개요
AgenticCP는 차세대 멀티 클라우드 플랫폼으로, React + TypeScript + Vite 기반의 현대적인 웹 애플리케이션입니다.

## 📁 프로젝트 구조

```
src/
├── components/          # 재사용 가능한 UI 컴포넌트
│   ├── common/         # 공통 컴포넌트 (ErrorBoundary, Loading, PageHeader)
│   ├── layout/         # 레이아웃 컴포넌트 (Header, Sidebar, MainLayout)
│   └── [feature]/      # 기능별 컴포넌트
├── pages/              # 페이지 컴포넌트
├── hooks/              # 커스텀 훅
├── services/           # API 서비스 레이어
├── store/              # 상태 관리 (Zustand)
├── types/              # TypeScript 타입 정의
├── utils/              # 유틸리티 함수
├── constants/           # 상수 정의
├── routes/             # 라우팅 설정
└── styles/             # 글로벌 스타일
```

## 🎨 디자인 시스템 규칙

### CSS 변수 사용 필수
```scss
// ✅ 올바른 예시
.component {
  color: var(--text-primary);
  padding: var(--space-4);
  border-radius: var(--radius-lg);
  background: var(--bg-primary);
}

// ❌ 잘못된 예시
.component {
  color: #333;
  padding: 16px;
  border-radius: 8px;
  background: #fff;
}
```

### 컴포넌트 스타일링 패턴
```scss
.component-name {
  // 기본 스타일
  display: flex;
  padding: var(--space-4);
  
  // 상태별 스타일
  &.active {
    background: var(--color-primary);
  }
  
  // 하위 요소
  .component-child {
    // 스타일
  }
  
  // 반응형
  @media (max-width: 768px) {
    // 모바일 스타일
  }
}
```

## ⚛️ React 컴포넌트 개발 규칙

### 1. 컴포넌트 구조
```typescript
import React from 'react';
import { ComponentProps } from '@/types';
import './Component.scss';

interface ComponentProps {
  title: string;
  children?: React.ReactNode;
  onAction?: () => void;
}

const Component: React.FC<ComponentProps> = ({ 
  title, 
  children, 
  onAction 
}) => {
  return (
    <div className="component">
      <h2 className="component-title">{title}</h2>
      {children}
    </div>
  );
};

export default Component;
```

### 2. Props 인터페이스 규칙
- 모든 Props는 명확한 타입 정의 필수
- Optional props는 `?` 사용
- 이벤트 핸들러는 `on` 접두사 사용
- children은 `React.ReactNode` 타입 사용

### 3. 컴포넌트 네이밍
- PascalCase 사용: `UserProfile`, `DashboardCard`
- 기능을 명확히 표현하는 이름 사용
- 접미사 규칙:
  - `Page`: 페이지 컴포넌트 (`DashboardPage`)
  - `Modal`: 모달 컴포넌트 (`ConfirmModal`)
  - `Form`: 폼 컴포넌트 (`LoginForm`)

## 🔧 TypeScript 개발 규칙

### 1. 타입 정의 패턴
```typescript
// 기본 인터페이스
export interface User {
  id: number;
  username: string;
  email: string;
  role: UserRole;
  createdAt: string;
  updatedAt: string;
}

// Union 타입
export type UserRole = 'SUPER_ADMIN' | 'TENANT_ADMIN' | 'CLOUD_ADMIN' | 'DEVELOPER';

// API 응답 타입
export interface ApiResponse<T = unknown> {
  success: boolean;
  data: T;
  message?: string;
  timestamp: string;
}

// 페이지네이션 타입
export interface PagedResponse<T = unknown> {
  content: T[];
  page: number;
  size: number;
  totalElements: number;
  totalPages: number;
  last: boolean;
  hasNext: boolean;
}
```

### 2. 타입 네이밍 규칙
- 인터페이스: PascalCase (`User`, `ApiResponse`)
- 타입 별칭: PascalCase (`UserRole`, `HttpStatus`)
- 제네릭: 단일 대문자 (`T`, `K`, `V`)
- 상수: UPPER_SNAKE_CASE (`API_BASE_URL`)

### 3. 타입 파일 구조
```typescript
// src/types/index.ts
/**
 * Common Types
 */
export interface ApiResponse<T = unknown> { ... }

/**
 * User Types
 */
export interface User { ... }
export type UserRole = ...;

/**
 * Cloud Types
 */
export interface CloudProvider { ... }
export interface Resource { ... }
```

## 🌐 API 서비스 개발 규칙

### 1. 서비스 구조 패턴
```typescript
import { apiRequest } from './api';
import { User, PagedResponse, PaginationParams } from '@/types';

/**
 * User Management Service
 */
export const userService = {
  /**
   * Get all users
   */
  getUsers: (params?: PaginationParams): Promise<PagedResponse<User>> => {
    return apiRequest.get<PagedResponse<User>>('/api/v1/users', { params });
  },

  /**
   * Get user by ID
   */
  getUserById: (id: number): Promise<User> => {
    return apiRequest.get<User>(`/api/v1/users/${id}`);
  },

  /**
   * Create user
   */
  createUser: (data: Partial<User>): Promise<User> => {
    return apiRequest.post<User>('/api/v1/users', data);
  },

  /**
   * Update user
   */
  updateUser: (id: number, data: Partial<User>): Promise<User> => {
    return apiRequest.put<User>(`/api/v1/users/${id}`, data);
  },

  /**
   * Delete user
   */
  deleteUser: (id: number): Promise<void> => {
    return apiRequest.delete<void>(`/api/v1/users/${id}`);
  },
};
```

### 2. API 메서드 네이밍 규칙
- `get[Resource]s`: 목록 조회 (`getUsers`)
- `get[Resource]ById`: 단일 조회 (`getUserById`)
- `create[Resource]`: 생성 (`createUser`)
- `update[Resource]`: 수정 (`updateUser`)
- `delete[Resource]`: 삭제 (`deleteUser`)

### 3. 에러 처리 패턴
```typescript
try {
  const response = await userService.getUsers();
  // 성공 처리
} catch (error) {
  console.error('사용자 목록 조회 실패:', error);
  // 에러 처리
}
```

## 🗃️ 상태 관리 규칙 (Zustand)

### 1. 스토어 구조 패턴
```typescript
import { create } from 'zustand';

interface StoreState {
  // 상태
  data: DataType[];
  isLoading: boolean;
  error: string | null;
  
  // 액션
  fetchData: () => Promise<void>;
  addData: (item: DataType) => void;
  updateData: (id: string, updates: Partial<DataType>) => void;
  deleteData: (id: string) => void;
  setLoading: (loading: boolean) => void;
  clearError: () => void;
}

export const useDataStore = create<StoreState>((set, get) => ({
  data: [],
  isLoading: false,
  error: null,

  fetchData: async () => {
    set({ isLoading: true, error: null });
    try {
      const response = await dataService.getData();
      set({ data: response, isLoading: false });
    } catch (error) {
      set({ 
        error: error instanceof Error ? error.message : '데이터 조회 실패',
        isLoading: false 
      });
    }
  },

  addData: (item) => set((state) => ({ 
    data: [...state.data, item] 
  })),

  updateData: (id, updates) => set((state) => ({
    data: state.data.map(item => 
      item.id === id ? { ...item, ...updates } : item
    )
  })),

  deleteData: (id) => set((state) => ({
    data: state.data.filter(item => item.id !== id)
  })),

  setLoading: (loading) => set({ isLoading: loading }),
  clearError: () => set({ error: null }),
}));
```

### 2. 스토어 네이밍 규칙
- 파일명: `[feature]Store.ts` (`userStore.ts`)
- 훅명: `use[Feature]Store` (`useUserStore`)
- 상태는 명사, 액션은 동사 사용

## 🎣 커스텀 훅 개발 규칙

### 1. 훅 구조 패턴
```typescript
import { useState, useEffect } from 'react';
import { userService } from '@/services/userService';
import { User } from '@/types';

/**
 * User Management Hook
 */
export const useUsers = () => {
  const [users, setUsers] = useState<User[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchUsers = async () => {
    setIsLoading(true);
    setError(null);
    try {
      const response = await userService.getUsers();
      setUsers(response.content);
    } catch (err) {
      setError(err instanceof Error ? err.message : '사용자 조회 실패');
    } finally {
      setIsLoading(false);
    }
  };

  useEffect(() => {
    fetchUsers();
  }, []);

  return {
    users,
    isLoading,
    error,
    refetch: fetchUsers,
  };
};
```

### 2. 훅 네이밍 규칙
- 파일명: `use[Feature].ts` (`useUsers.ts`)
- 훅명: `use[Feature]` (`useUsers`)
- 반환값은 객체 형태로 구조분해할당 가능하게 구성

## 🛠️ 유틸리티 함수 개발 규칙

### 1. 유틸리티 구조 패턴
```typescript
/**
 * Date Formatting Utilities
 */
export const formatDate = (date: Date | string): string => {
  return new Date(date).toLocaleDateString('ko-KR');
};

export const formatRelativeTime = (date: Date | string): string => {
  return dayjs(date).fromNow();
};

/**
 * Number Formatting Utilities
 */
export const formatNumber = (value: number, decimals = 0): string => {
  return value.toLocaleString('ko-KR', {
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  });
};

export const formatCurrency = (value: number, currency = 'KRW'): string => {
  return new Intl.NumberFormat('ko-KR', {
    style: 'currency',
    currency,
  }).format(value);
};
```

### 2. 유틸리티 네이밍 규칙
- 파일명: 기능별로 분리 (`format.ts`, `validator.ts`, `storage.ts`)
- 함수명: 동사 + 명사 (`formatDate`, `validateEmail`)
- 순수 함수로 작성 (부작용 없음)

## 📝 상수 정의 규칙

### 1. 상수 구조 패턴
```typescript
/**
 * Application Constants
 */
export const APP_NAME = import.meta.env.VITE_APP_NAME || 'AgenticCP';
export const APP_VERSION = import.meta.env.VITE_APP_VERSION || '1.0.0';

/**
 * API Constants
 */
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080';
export const API_TIMEOUT = Number(import.meta.env.VITE_API_TIMEOUT) || 30000;

/**
 * Storage Keys
 */
export const STORAGE_KEYS = {
  TOKEN: 'agenticcp_token',
  REFRESH_TOKEN: 'agenticcp_refresh_token',
  USER_INFO: 'agenticcp_user_info',
} as const;

/**
 * Route Paths
 */
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  USERS: '/users',
} as const;
```

### 2. 상수 네이밍 규칙
- UPPER_SNAKE_CASE 사용
- 객체는 `as const` 사용하여 타입 안정성 확보
- 관련 상수는 객체로 그룹화

## 🚦 라우팅 개발 규칙

### 1. 라우트 구조 패턴
```typescript
import { Routes, Route, Navigate } from 'react-router-dom';
import { lazy, Suspense } from 'react';
import PrivateRoute from './PrivateRoute';
import { ROUTES } from '@/constants';

// Lazy load pages
const DashboardPage = lazy(() => import('@/pages/dashboard/DashboardPage'));
const UsersPage = lazy(() => import('@/pages/users/UsersPage'));

const AppRoutes = () => {
  return (
    <Suspense fallback={<Loading />}>
      <Routes>
        {/* Public routes */}
        <Route path={ROUTES.LOGIN} element={<LoginPage />} />

        {/* Protected routes */}
        <Route element={<PrivateRoute />}>
          <Route element={<MainLayout />}>
            <Route path={ROUTES.HOME} element={<Navigate to={ROUTES.DASHBOARD} replace />} />
            <Route path={ROUTES.DASHBOARD} element={<DashboardPage />} />
            <Route path={ROUTES.USERS} element={<UsersPage />} />
          </Route>
        </Route>

        {/* 404 page */}
        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </Suspense>
  );
};
```

### 2. 라우팅 규칙
- 모든 경로는 `ROUTES` 상수 사용
- 페이지 컴포넌트는 lazy loading 적용
- 보호된 라우트는 `PrivateRoute`로 감싸기
- 404 페이지는 마지막에 배치

## 🎨 스타일링 개발 규칙

### 1. SCSS 파일 구조
```scss
/* ===========================================
   COMPONENT NAME
   =========================================== */

.component-name {
  // 기본 스타일
  display: flex;
  padding: var(--space-4);
  
  // 상태별 스타일
  &.active {
    background: var(--color-primary);
  }
  
  // 하위 요소
  .component-child {
    // 스타일
  }
  
  // 반응형
  @media (max-width: 768px) {
    // 모바일 스타일
  }
}
```

### 2. 스타일링 규칙
- CSS 변수 우선 사용
- BEM 방법론 또는 의미있는 클래스명 사용
- 컴포넌트별로 분리된 SCSS 파일 사용
- 반응형 디자인 고려

## 📋 코드 품질 규칙

### 1. 네이밍 컨벤션
- **변수/함수**: camelCase (`userName`, `getUserData`)
- **상수**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **컴포넌트**: PascalCase (`UserProfile`)
- **파일명**: kebab-case (`user-profile.tsx`)

### 2. 주석 규칙
```typescript
/**
 * 사용자 정보를 조회합니다
 * @param userId 사용자 ID
 * @returns 사용자 정보 Promise
 */
export const getUserById = async (userId: number): Promise<User> => {
  return apiRequest.get<User>(`/api/v1/users/${userId}`);
};
```

### 3. 에러 처리 규칙
```typescript
try {
  const result = await apiCall();
  // 성공 처리
} catch (error) {
  console.error('API 호출 실패:', error);
  // 사용자에게 적절한 에러 메시지 표시
  throw error;
}
```

## 🔍 개발 도구 및 설정

### 1. 필수 VS Code 확장
- TypeScript Importer
- ES7+ React/Redux/React-Native snippets
- Prettier - Code formatter
- ESLint

### 2. 코드 포맷팅
- Prettier 설정 사용
- 세미콜론 사용
- 작은따옴표 사용
- 2칸 들여쓰기

### 3. Git 커밋 메시지 규칙
```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅
refactor: 코드 리팩토링
test: 테스트 추가/수정
chore: 빌드 설정 변경
```

## 🚀 배포 및 빌드 규칙

### 1. 환경 변수 관리
```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_NAME=AgenticCP Dev

# .env.production
VITE_API_BASE_URL=https://api.agenticcp.com
VITE_APP_NAME=AgenticCP
```

### 2. 빌드 최적화
- 코드 스플리팅 적용
- 이미지 최적화
- 번들 크기 모니터링

## 📚 참고 자료

### 1. 공식 문서
- [React 공식 문서](https://react.dev/)
- [TypeScript 공식 문서](https://www.typescriptlang.org/)
- [Vite 공식 문서](https://vitejs.dev/)
- [Zustand 공식 문서](https://zustand-demo.pmnd.rs/)

### 2. 디자인 시스템
- 프로젝트 내 `.cursorrules` 파일 참조
- CSS 변수 기반 디자인 토큰 사용

이 가이드를 따라 일관되고 유지보수 가능한 코드를 작성하세요! 🚀